# Remote Access Example

This example demonstrates using the Foxglove remote access gateway to stream
camera images for live visualization through the Foxglove platform.

## Prerequisites

Set the `FOXGLOVE_DEVICE_TOKEN` environment variable with your device token from
the Foxglove platform.

## Running

```bash
FOXGLOVE_DEVICE_TOKEN=your-token-here uv run main.py
```
