[project]
name = "asset-server"
version = "0.0.0"
description = "Example of a WebSocket and remote access asset server"
authors = [{ name = "Foxglove", email = "support@foxglove.dev" }]
requires-python = ">=3.10"
readme = "README.md"
dependencies = ["foxglove-sdk>=0.26.0"]

[dependency-groups]
dev = ["mypy>=1,<2"]

[tool.uv]
package = false
